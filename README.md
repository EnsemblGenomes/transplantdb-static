# transplantdb-static

Static HTML version of www.transplantdb.eu

## Build and run Apache docker container
```
docker build -t transplantdb .
docker run -d -p 80:80 transplantdb
```
Now browse http://localhost


